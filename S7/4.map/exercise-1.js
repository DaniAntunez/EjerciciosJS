const users = [
  { id: 1, name: "Abel" },
  { id: 2, name: "Julia" },
  { id: 3, name: "Pedro" },
  { id: 4, name: "Amanda" },
];

const userName = users.map(user => user.name);

//const userName = users.map((usuario) => {
//    return userName.name}
//);

//users.forEach(user => {
//  user.name
//});

//for (const user of users) {
//  console.log(user.name);
//};

console.log(userName);
